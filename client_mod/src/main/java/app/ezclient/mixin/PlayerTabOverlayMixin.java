package app.ezclient.mixin;

/** Tab list stays clean: the logo renders next to the world name tag only. */
@org.spongepowered.asm.mixin.Mixin(net.minecraft.client.gui.components.PlayerTabOverlay.class)
abstract class PlayerTabOverlayMixin {
}
